when starting on new machine 
sudo vim into files, change path to your path
sudo cp the files to /lib/systemd/system/ 
then, sudo systemctl start *******
sudo systemctl enable *******
******* is the filename (loginServer.service)

ONLY START ERROR LISTENER ON RABBITMQ MACHINE

ONLY START LOGIN AND MATCH LISTENERS ON BACKEND MACHINE
